# ShopEase React + Vite E-Commerce

Tech: React.js, Vite, CSS3, JavaScript ES6+.

Features: 12 products, product images, search, category filters, sorting, cart, quantity controls, wishlist, LocalStorage and responsive UI.

Run:
1. npm install
2. npm run dev

Product images use remote Unsplash URLs, so internet is required for images.
